# Cost Controls

All expensive Azure resources are **opt-in by design**.

## Toggles
- deployFirewall (default: false)
- deployAks (default: false)
- deployDefender (default: false)
- deploySentinelAnalytics (default: false)

This ensures safe experimentation without surprise costs.
